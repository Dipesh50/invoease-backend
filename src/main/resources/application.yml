spring:
  datasource:
    url: jdbc:mysql://localhost:3306/invoease?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: Dipesh12@@&
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
server:
  port: 8080